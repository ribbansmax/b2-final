<%= form_with scope: 'operation', url: operations_path, method: "post", local: true do |f| %>
  <%= f.hidden_field :surgery_id, value: params[:id]%>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "Add Doctor"%>
<% end %>